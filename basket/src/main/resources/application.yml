server:
  port: 8083
spring:
  application:
    name: basket
  datasource:
    url: jdbc:mysql://localhost:3306/basket?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 2461.Atay
    username: root
  jpa:
    hibernate:
      ddl-auto: update
